import styled from "styled-components";

export const NodeHeader = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 40px;
  background-color: #f1f1f1;
  padding: 8px;
  border-top-left-radius: 8px;
  border-top-right-radius: 8px;
  cursor: pointer;
`;


export const NodeWrapper = styled.div`
  position: relative;
  resize: both;
  background-color: #fff;
  box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.2);
  border-radius: 8px;
`;

export const NodeTitle = styled.div`
  font-size: 16px;
  font-weight: bold;
  display: flex;
  align-items: center;
  margin-left: 10px;
`;

export const NodeContent = styled.div`
  padding: 16px;
`;

export const NodeForm = styled.div`
  display: flex;
  flex-direction: column;
  gap: 8px;
`;

export const NodeLabel = styled.label`
  font-size: 14px;
  font-weight: bold;
`;

export const NodeTextarea = styled.textarea`
  padding: 8px;
  border: none;
  border-radius: 4px;
  font-size: 14px;
  background-color: #f9f9f9;
  resize: vertical;
  min-height: 80px;
  max-height: 300px;
`;

export const NodeIcon = styled.div`
  width: 20px;
  height: 20px;
`;

export const NodeContainer = styled.div`
  position: relative;
  resize: both;
  background-color: #fff;
  box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.2);
  border-radius: 8px;
`;

export const NodeLogsText = styled.p`
  font-size: 1em;
  padding: 10px;
  margin: 0;
`;

export const NodeLogs = styled.div<{ showLogs: boolean }>`
  border-radius: 0 0 8px 8px;
  font-size: 0.85em;
  padding: 10px;
  overflow: hidden; /* Prevents content from overflowing component */
  transition: height 0.2s ease-out; /* Adds a transition for opening/closing the section */
  background-color: #85c4eb;
  color: #fff;
  height: ${({ showLogs }) => (showLogs ? 'fit-content' : 'fit-content')};
  cursor: pointer;
`;


